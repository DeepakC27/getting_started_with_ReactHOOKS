import Input_ from './Input'
import Button_ from './Button'
import PageLoader_ from './PageLoader'

export const Input = Input_
export const Button = Button_
export const PageLoader = PageLoader_

export default {
  Input,
  Button,
  PageLoader
}
